/**
 * The plans a licence can grant.
 *
 * Reconciled 2026-09-09 against the code that actually mints keys, editor-api's
 * `common/license-utils.js`. It issues a key for exactly `pro`, `premium` and
 * `enterprise` (`requiresLicense`), lowercased from the Stripe product's
 * `plan_type` metadata, falling back to `pro`.
 *
 * `community` is in this type but will never appear in a key, deliberately:
 * that tier is issued none, so the Community state IS the unlicensed state.
 * Keeping it in the ordering gives `planAtLeast('community')` the useful
 * meaning "holds any licence at all", which is a real gate even though no
 * payload carries the word.
 */
export type ApexPlan = 'community' | 'enterprise' | 'premium' | 'pro';
/**
 * Plans in ascending order of what they unlock. `enterprise` is the OEM and
 * Embedded SKU, which sits above `premium` because its unit is the deployment
 * rather than the developer.
 */
export declare const PLAN_ORDER: readonly ApexPlan[];
/**
 * Plan strings that may exist in already-issued keys but are not plan names.
 *
 * DELIBERATELY EMPTY, and it should stay that way.
 *
 * The reconciliation found only one source of off-vocabulary strings:
 * `tools/scripts/licence-generator.js`, the manual signing tool, whose `--plan`
 * used to default to `standard` and accept any string at all. Production has
 * never emitted `standard` or `trial`. That script now requires an explicit
 * plan and validates it, so no new key can carry one.
 *
 * Old manually-signed keys may still be out there, and nothing records which
 * customer holds which. That is exactly what the fail-open branch in
 * `planAtLeast` is for: an unmapped string on a valid key grants the feature
 * and warns once, so a hand-issued key cannot lock a customer out of what they
 * paid for. Mapping `standard` to a rank would do the opposite, on a guess.
 *
 * Add an entry only from the issued-key record, never from what a string looks
 * like it ought to mean.
 */
export declare const PLAN_ALIASES: Readonly<Record<string, ApexPlan>>;
/**
 * Resolve a raw `LicenseData.plan` string to a known plan.
 *
 * Case and surrounding space are ignored, because a plan string is typed by a
 * human at issuance. Returns `null` for anything not a SKU name or a mapped
 * alias; callers must decide what an unknown plan means rather than guess here.
 */
export declare function normalisePlan(raw: null | string | undefined): ApexPlan | null;
/**
 * The plan the current licence grants, or `null` when there is no valid licence
 * or its plan string is not recognised.
 *
 * `null` is two different situations on purpose: use `planAtLeast` to gate, and
 * this only to display. Anything that renders this must cope with `null`.
 */
export declare function currentPlan(): ApexPlan | null;
/**
 * Whether the current licence covers a feature needing at least `required`.
 *
 * Three outcomes, and the third is the important one:
 *
 * 1. No valid licence: `false`, always. This is the one hard boundary, and it
 *    is the same verdict that already drives the watermark.
 * 2. Valid licence with a recognised plan: rank comparison.
 * 3. Valid licence with an UNRECOGNISED plan: `true`, and the string is warned
 *    about once.
 *
 * Case 3 is fail-open by choice. The holder has a signed, unexpired key, so
 * they paid for something; denying them a feature over a string we failed to
 * map is a support incident and a refund conversation, while granting one is a
 * feature given to an existing customer. Client-side gating here is deterrence
 * and honest-customer compliance, not DRM, so the asymmetry is not close. It
 * also means switching gating on cannot downgrade anyone before PLAN_ALIASES is
 * reconciled against the issued-key record.
 *
 * Never throws, and never awaits: products call this during render.
 */
export declare function planAtLeast(required: ApexPlan): boolean;
//# sourceMappingURL=tier.d.ts.map